<Page x:Class="WpfApp1.slidebarTest"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="slidebarTest">

    <Grid>

        <Slider Minimum="0" Maximum="100" Value="50" TickFrequency="1" IsSnapToTickEnabled="True" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical" Height="300"
                ValueChanged="Slider_ValueChanged"/>
        <TextBlock Text="{x:Static local:StaticData.TestWord}"   HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24" Foreground="#ffffff"/>
        <TextBlock Text="{x:Static local:StaticData.TestWord2}" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="24" Foreground="#ffffff"/>
        <TextBlock Name="TestValue" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="24" Foreground="#ffffff"/>
    </Grid>
</Page>
